# Picom snippet for i3space (include from picom.conf)
# include "i3space.conf"

rules: ({
  match = "class_g = 'i3space'";
  opacity = 0.92;
  blur-background = true;
  blur-strength = 5;
  fade = false;
  shadow = false;
});
